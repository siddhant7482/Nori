node_modules
.next
out
build
.DS_Store
*.pem

# env files: .env.example is the committed template
.env*
!.env.example

*.tsbuildinfo
next-env.d.ts

# Tesseract language data, downloaded on first run (receipt engine)
.cache/

# Third-party receipt dataset: real receipts, not ours to redistribute.
# The benchmarks in receipts/scripts/bench-*.mts read from here; point
# --dir elsewhere or drop the images back in to reproduce them.
receipts_train/
